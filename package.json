{
  "name": "spacedout-monorepo",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run start --prefix server\" \"npm run dev --prefix client\"",
    "install": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}